[project]
name = "site-crawl"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "flask>=3.0.0",
    "requests>=2.31.0",
    "beautifulsoup4>=4.12.0",
    "gunicorn~=23.0.0",
]

[tool.vercel]
entrypoint = "backend.app:app"
